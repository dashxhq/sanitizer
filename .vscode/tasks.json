{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "all: clippy",
            "group": "build",
            "type": "shell",
            "command": "cargo watch -x clippy",
            "problemMatcher": "$rustc-watch",
            "isBackground": true,
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "presentation": {
                "panel": "dedicated",
                "clear": true
            }
        }
    ]
}
